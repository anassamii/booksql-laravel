Migration + Models?(relations) + Schema.graphql(Routes + Models + Controllers) + app/http/GraphQL/Queries(Controllers, for custom commands, Optionel) + Factory/Seeds (Dummy data, Optionel)

php artisan serve
